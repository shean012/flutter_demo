import 'dart:ui';

import 'package:flutter/material.dart';

class AppStyle {
  static const Radius primaryRaduis = Radius.circular(6.0);

  static const BorderRadius primaryBorderRaduis =
      BorderRadius.all(Radius.circular(6.0));
}
